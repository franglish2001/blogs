services:
  db:
    image: postgress:15.8
    environment:
      - POSTGRES_USER = blog
      - POSTGRES_PASSWORD = blog
    volumes:
      - pg_data_blog:/var/lib/postgresql/data
      - ./init-script:/docker-entrypoint-initdb.d
    ports:
      - 5459:5432

volumes:
  pg_data_blog:

