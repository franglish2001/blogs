name: pipeline ci/cd
on:
  push:
    branches:
      - '*'

  pull_request:
    branches:
      - '*'

jobs:
  check_syntaxe:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.12']

    steps:
      - uses: actions/checkout@v2
      - name: python set-up
        uses: actions/setup-python@v2
        with:
          python-version: 3.12

      - name: install dependances
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install flake8

      - name: check code line with flake8
        run: |
          flake8 . --exit-zero

  test:
    runs-on: ubuntu-latest
    needs: ['check_syntaxe']
    services:
      postgres:
        image: postgres:15.8
        ports:
          - 5459:5432
        env:
          POSTGRES_USER: blog
          POSTGRES_PASSWORD : blog
          POSTGRES_DB: blog_test

    steps:
      - uses: actions/checkout@v2
      - name: check syntaxe
        uses: actions/setup-python@v2
        with:
          python-version: 3.12

      - name: install dependance for test
        run: |
          make install_dev
      - name: run tests
        run: |
          make tests

  build_image:
    name: build and push docker image
    runs-on: ubuntu-latest
    needs: 'test'

    steps:
      - uses: actions/checkout@v2
      - name: docker login
        env:
          username: ${{secrets.DOCKERHUB_USER}}
          password: ${{secrets.DOCKERHUB_PASSWORD}}
        run: |
          docker login -u $username -p $password
      - name: docker buil and push
        run: |
          docker build . -t dounoh0/blog:latest
          docker push dounoh0/blog:latest
